[build]
builder = "dockerfile"

[deploy]
startCommand = "bash -c 'alembic upgrade head && exec uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 2 --proxy-headers'"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
